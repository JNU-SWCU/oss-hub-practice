generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

// Product models are intentionally deferred until the PRD domain vocabulary is stable.
